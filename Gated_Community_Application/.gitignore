# ── Build output ──────────────────────────────────────────────
target/
*.war
*.jar
*.class

# ── Database credentials (never commit the real config) ───────
# Copy hibernate.cfg.xml.template → hibernate.cfg.xml and fill in your credentials.
src/main/resources/hibernate.cfg.xml

# ── IDE files ─────────────────────────────────────────────────
.idea/
*.iml
*.iws
*.ipr
.project
.classpath
.settings/
*.launch
*.factorypath
nbproject/
nb-configuration.xml

# ── Eclipse / STS ─────────────────────────────────────────────
.metadata/
.springBeans
.sts4-cache/

# ── VS Code ───────────────────────────────────────────────────
.vscode/

# ── OS files ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Logs ──────────────────────────────────────────────────────
*.log
logs/

# ── Maven wrapper (optional — include if you add one) ─────────
# !.mvn/wrapper/maven-wrapper.jar

# ── Temporary files ───────────────────────────────────────────
*.tmp
*.swp
*~
